CREATE TABLE Camera (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  rover_id INTEGER NOT NULL,
  full_name TEXT NOT NULL
);

upsert:
UPDATE Camera
SET name = ?,
    rover_id = ?,
    full_name = ?
WHERE id = ?;